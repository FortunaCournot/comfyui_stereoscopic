{
  "version": "1",
  "blueprint": "ffmpeg-v2v",
  "options": "-map 0 -vf 'pad=ceil(iw/2)*2:ceil(ih/2)*2' -c:v libx264 -pix_fmt yuv420p -crf 18 -preset medium -c:a copy -c:s copy -movflags +faststart",
  "input": "video",
  "output": "video",
}
